# Screenshots

Images referenced by the root [README.md](../../README.md). Save PNGs here with these exact names:

| File | Shot |
| --- | --- |
| `call-screen.png` | Customer call screen (`/`) — the "Hello. Please tell us how we can help today." prompt |
| `admin.png` | Admin dashboard (`/admin`) — agent list with availability toggles |
| `analytics.png` | Analytics dashboard (`/analytics`) — metric tiles and charts |

Capture at ~1440px wide on a light background for consistency.
