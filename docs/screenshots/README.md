# Screenshots

Images referenced by the root [README.md](../../README.md).

| File | Shot | Status |
| --- | --- | --- |
| `call-screen.png` | Customer call screen (`/`) — the "Hello. Please tell us how we can help today." prompt | ✅ in place |
| `admin.png` | Admin dashboard (`/admin`) — agent list with availability toggles | not captured yet |
| `analytics.png` | Analytics dashboard (`/analytics`) — metric tiles and charts | not captured yet |

Capture at ~1440px wide on a light background for consistency, and crop out the browser chrome. Once `admin.png` and `analytics.png` exist here, add them to the root README under "The three screens" — don't reference them before then, since GitHub renders a missing image as a broken-link icon.
